use "std/io.cnd";
use "std/alloc.cnd";

fn use_resource() {
    let ptr = alloc(1024);
    defer dealloc(ptr);  
    
    unsafe {
        ptr[0] = 42; 
        ptr[1] = 43;
    }
}

fn main() -> i32 {
    use_resource();
    println("Defer worked!");
    return 0;
}
